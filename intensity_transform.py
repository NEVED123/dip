def apply_intensity_transform(img, transform):
    """
        Apply an intensity transform to an image.
    """
    return img

def histrogram_equalization(img):
    """
        Perform histogram equalization on an image.
    """
    histo_equal = lambda x: x
    return apply_intensity_transform(img, histo_equal)
